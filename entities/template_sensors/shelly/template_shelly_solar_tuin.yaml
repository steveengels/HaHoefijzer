name: template_shelly_solar_tuin
unit_of_measurement: "W"
unique_id: template_shelly_solar_tuin
state_class: measurement
device_class: power
state: >
  {% set val = (states('sensor.shellyemtuin_em0_power') | float) %}
  {% if val < 0 %}
    {{ (val * -1) * 1000 }}
  {% else %}
    0
  {% endif %}