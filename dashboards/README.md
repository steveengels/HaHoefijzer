# Dashboards

This folder stores Home Assistant dashboard YAML and related dashboard exports.

- `overview.yaml` is the combined Hoefijzer dashboard shown in the Home Assistant sidebar.
- `hoefijzer.yaml` is the older Hoefijzer engineering dashboard kept as a hidden backup.
- `energy_diagnostics.json` is the Home Assistant energy dashboard diagnostics export.
