# Dashboards

This folder stores Home Assistant dashboard YAML and related dashboard exports.

- `overview.yaml` is the exported Overview dashboard registered in `configuration.yaml`.
- `hoefijzer.yaml` is a draft engineering dashboard for energy, EV charging, and climate experiments.
- `energy_diagnostics.json` is the Home Assistant energy dashboard diagnostics export.
