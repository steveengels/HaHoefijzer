template:
  sensor: !include_dir_list ../entities/template_sensors
  binary_sensor: !include_dir_list ../entities/template_binary_sensors
  # - platform: template
  #   name: "Internet Weather Station"
  #   condition_template: "{{ states('weather.forecast_home') }}"
  #   temperature_template: "{{ state_attr('weather.forecast_home', 'temperature') | float }}"
  #   temperature_unit: "°C"
  #   humidity_template: "{{ state_attr('weather.forecast_home', 'humidity') | float }}"
  #   #forecast_template: "{{ state_attr('weather.my_region', 'forecast') }}"
  #   wind_speed_template: "{{ state_attr('weather.forecast_home', 'wind_speed') }}"
  #   wind_speed_unit: "m/s"
